{
  "name": "lecture-studio-audience",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "build": "wrangler deploy --dry-run --outdir ../.local/worker-build"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
